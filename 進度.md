# 9x9game（小虎乘法大冒險）進度

給國小學生練九九乘法表的網頁遊戲。**一個大廳 ＋ 七個遊戲**，進度全部存在瀏覽器裡。

---

## 已完成（2026-08-23 最新，換電腦接手看這段就好）
- **主線叫「勇者出擊」**（`battle.html`，原「打怪大作戰」）
- **兩種模式**：練習模式（出題）／娛樂模式（不出題，純操作）。
  **只能在大廳選**，遊戲頁唯讀；切到娛樂模式要密碼 **5407**（`MODE_PASSWORD`）
- **題型可選**：九九乘法／加法／減法／加減混合／全部混合（20 以內給低年級）
- **登入與雲端存檔**：帳號**跟 lunquiz（小綸出題小幫手）共用同一套**，
  後端在 `school/lunquiz/worker`（`linquiz-api`），存檔在它的 D1 表 `game_saves`
- **本地四個存檔位**（`_s1`～`_s4`），答題統計與最佳成績跟著分格
- **角色可自己取名字**（`Save.d.pname`）
- **主動技能兩顆**：🌀 旋風斬（Lv3／冷卻 9 秒）、💚 治療術（Lv6／冷卻 22 秒）
- **怪 15 隻**，行為分四種：⚡ 出手快／🛡 有護甲／🎯 遠程投射物／🐝 召喚護盾；出手前 1.2 秒有預告
- **逐幀動畫**：小虎姬、黏黏怪、紫金巨龍（`assets/spr_*.webp`）
- **角色 6 個**（含狂戰小虎、魔導小虎）
- **效能自動降級**（`Fx`）：偵測掉幀就關掉吃效能的層、逐幀壓 12fps

## 目前狀態
- **強化卷軸改成固定價格 10 萬**（Steve 否決隨等級漲，要固定但不能太好取得），
  掉落率照天堂精神走稀有（小怪 2%、Boss 25%，王者職業翻倍）
- **職業定名「王者」**（不是虎王，也不是金冠王者，Steve 挑到滿意為止）

## 跟 Gemini 深度討論後的可玩度改版（2026-08-23，帳號 A，5 輪）
Steve 反饋：只是一直點不好玩，要參考楓之谷／傳說對決加操作深度；裝備部位要擴充；
數值平衡要抓；卷軸掉落率、價格都要照天堂的精神調。

**先澄清一個我自己算錯的數字**：先前口頭講「新手到滿等差 550 倍」是我用錯公式
（誤把三個裝備部位都當攻擊主屬性去疊乘）。用遊戲真正的 `Save.power()` 實測：
Lv50（+3 安全強化）29.3 倍、Lv200（+6 安全）173.6 倍、Lv200（+10 賭到底）343.4 倍——
都落在 Gemini 建議的健康區間（Lv50 抓 25~35 倍、頂級抓 400~600 倍），**現有公式其實沒有失控**，
沒有大改。

**裝備擴充成 4 部位**（Gemini 建議只加一個，不要做成天堂那種 10 幾個部位）：
武器（攻擊）／防具（血量）／**鞋子（新增，技能冷卻縮短，上限 30%）**／飾品（爆擊）。
鞋子的 cdr 跟等級關係壓得很平（`3 + 等級×0.05`），不然後期一雙鞋就把冷卻砍光，
連段的節奏感會被削掉。

**新增操作深度（都接在既有系統上，沒加新 UI 元素）**：
- **完美閃避**：閃避隨便按都躲得掉，但在怪物出手前**最後 0.6 秒內**按才是「完美」——
  剛好接在原本就有的出手預告（`warnMob`，剩 1.2 秒開始亮警告）上。完美閃避多回必殺、
  進 0.5 秒慢動作、下一擊變成 1.8 倍傷害的「反擊」
- **印記引爆連段**：新增第三顆技能「破魂突刺」（Lv4／冷卻 7 秒），打上「破綻」印記 4.5 秒，
  這段時間內放「旋風斬」會引爆造成 1.9 倍加成傷害。這是《傳說對決》「先放這個再接那個」的精神，
  但改成單體版本——**我們戰鬥畫面永遠只有一隻怪**，跟參考站不同，Gemini 一開始給的多怪聚怪
  方案（冰渦拉怪）用不上，換成印記＋引爆才符合我們的設計
- 現在打怪頁是 5 顆按鈕（攻擊／閃避／破魂突刺／旋風斬／治療術），比 Gemini 建議的「≤4 顆」
  多一顆，但手機測過排版沒問題，操作深度換一顆按鈕的空間划算

**沒有做的部分**（Gemini 給了但先不做，記在下一步）：
- 節奏連段（第二擊要卡精準時機才觸發加成）、攻擊鈕滑動手勢派生——工程量較大，先觀察
  上面這批的實際手感再決定要不要加
- 每日任務／簽到、怪物圖鑑被動、每日變異塔（用日期當隨機種子）——這三個是「長期黏著度」系統，
  跟本次「操作不好玩」的訴求關聯較低，排到下一輪

**驗證**：八頁無錯誤無溢出；實測印記引爆（63 血一擊清空）、完美閃避（躲過傷害＋反擊 41 傷害）、
鞋子 cdr 生效（9000ms 冷卻在 22% 縮短下變成 7020ms，實測誤差在合理範圍）、
300 次模擬掉落確認四個部位都會出現。

- 線上：**https://9x9game.pages.dev**（發布到 `steveedu711-oss/9x9game` master）
- 八頁 × 桌機／手機驗過：無 JS 錯誤、無 404、無橫向溢出
- 手機模擬（CPU ×4）：中位 16.7ms、長幀 1.4%
- Steve 2026-08-23 已實際註冊登入成功
- 素材產線：影片 → `tools/sprite_char.py`；單張圖 → `tools/cut_art.py`；特效壓黑 → `tools/fix_fx.py`

## 下一步
1. `fx_poison` 這張特效還沒用上（可做成毒霧技能或某隻怪的招式）
3. 其他 9 隻怪與 4 個角色還是靜圖，補逐幀要再生影片（一天 3 部）
4. `findnum.html` 沒吃 common.js，那頁的紀錄不會上雲

## 職業重做（2026-08-23，照《天堂》分類，每個都要有特色）
數值差只是底，真正的差別在**每個職業一條別人沒有的機制**（`HEROES` 的 trait 欄位）。

| 職業 | 數值 | 別人沒有的特色 |
|---|---|---|
| 👑 王族 | 攻 +2／血 +10／爆 +2%／魔法 ×1.1 | **金幣 +35%、卷軸掉落率 +12%** |
| 🛡️ 騎士 | 攻 -1／血 +36 | **受到傷害 -20%，每次被打回 10 點必殺** |
| 🏹 妖精 | 攻 +1／血 -8／爆 +15% | **爆擊時額外回 8 點必殺** |
| 🔮 法師 | 攻 -2／血 -12／魔法 ×1.7 | **連擊魔法提早兩連就放**（1 連火球、3 連冰霜…） |
| 🐉 龍騎士 | 攻 +8／血 +60／魔法 ×0.9 | 又硬又痛，但**技能冷卻 ×1.3** |
| ✨ 幻術師 | 攻 -2／魔法 ×1.35／必殺 +8 | **技能冷卻 -35%** |

- 特色分別接在：`killMob`（金幣、卷軸）、`mobAttack`（減傷、被打回必殺）、
  `applyHit`（爆擊回必殺）、`spellFor`（提早幾連）、`useSkill`（冷卻倍率）
- ⚠ 舊存檔的角色代號用 `HERO_ALIAS` 對應（tiger→royal、archer→elf、berserk→dragon），
  不然換版之後大家的角色會被打回預設值

## 天堂式強化與 200 級（2026-08-23，Steve 指定）
**強化規則整套改成《天堂》那樣**：安定值 +6，過了就會爆。

- **強化要用「強化卷軸」**（`Save.d.scroll`）：掉落照天堂的精神走**稀有**——
  小怪 2%、Boss 25% 掉一張（王者職業翻倍），商店（裝備視窗裡）一張 200 金幣。
  2026-08-23 Steve 罵過「掉落率不要這麼高」：第一版小怪三成根本是發免錢的，
  稀有掉落＋商店販售才是正確的雙管道
- **+6 以前一定成功**（`ENH_SAFE = 6`），**+7 起失敗會爆炸、裝備直接消失**
  （成功率 60／50／40／30%）。第一次進危險區一定會跳警告視窗，不能手滑
- **過了安定值每一級都要明顯更強，而且越往上跳越多**（Steve 特別交代）：
  安定值內每級 +12%；+7 加 35%、+8 加 45%、+9 加 60%、+10 加 80%。
  攻擊 20 的武器：+6 是 34、+7 跳到 41、+8 → 50、+9 → 62、+10 → 78
- 強化按鈕會直接寫「攻擊 34 → 41（+7）」，讓人看得到值不值得賭

**等級開放到 200**（`MAX_LV = 200`）
- 曲線：Lv1～4 寫死 → Lv5～29 每級 ×1.20 → Lv30～99 線性 0.08 → Lv100+ 線性 0.10。
  Lv200 累計約 4,000 萬經驗（第一版算過 9.5 億，那不是難度是絕望，砍掉重調）
- **怪物的血量成長跟經驗成長刻意脫鉤**：血量 ×1.06^關卡、攻擊 ×1.05、**經驗 ×1.15**、金幣 ×1.10。
  玩家戰力大致線性成長，血量若也用 1.15，第 60 輪會變成幾十萬血打不動；
  但經驗需要指數才追得上 200 級。高關卡＝**經驗效率高**，不是變成肉山
- 模擬節奏（每輪 15 隻）：第 1 輪 Lv10、第 15 輪 Lv30、第 43 輪 Lv100、第 62 輪滿等
- 數字大了要好讀：新增 `fmtN()`（1.5萬／23萬／1.23億），血條、金幣、傷害、結算都吃

## 升級曲線改寫（2026-08-23，參考放置天堂）
原本是 `40 + 等級×35` 的直線，後期升級快得沒有成就感。讀了參考站
（`shines871/idle-lineage-class` 的 `js/00-data.js`）的做法後改成三段：

| 段 | 做法 | 為什麼 |
|---|---|---|
| Lv1～4 | 寫死 8／22／45／80 | 新玩家頭幾分鐘一定要看到等級一直跳 |
| Lv5～34 | `80 × 1.26^(lv-4)` | 固定倍率穩定爬升 |
| Lv35～59 | 從 Lv35 的值開始線性成長 | 不線性化後段會變天文數字（參考站是 Lv70 之後改 `(lv²+1)×固定值`） |

- **等級上限 60**（`MAX_LV`），滿等後經驗歸零、大廳顯示「已經滿等 Lv 60」、血條標「（滿等）」
- ⚠ 參考站有一條很值得抄的原則：**後期變慢一律靠「需求變高」，不要調低打怪拿到的經驗**
  （它的 `getExpGainMult` 註解就寫著「取消高等經驗遞減，遞減效果改由 getExpReq 承擔」）。
  拿到的變少玩家會覺得被扣，需求變高則是自然的難度曲線
- 實測節奏：純玩模式打 5 隻怪就到 Lv5；Lv20 累計約 1.5 萬經驗、Lv30 約 15.7 萬

## 裝備強化（2026-08-23）
金幣終於有用途了。天堂式的高風險高報酬，但**裝備永遠不會消失**（小學生也在玩，爆掉太傷）。

- 每強化一級主屬性 **+12%**，上限 **+10**
- 成功率：+0～+2 必成功，之後 85／75／65／55／45／38／30 遞減
- 費用：`(30 + 分數×0.35) × 1.55^等級`，越後面越貴，才有「要不要賭」的感覺
- 失敗：+0～+3 等級不變，+4～+6 掉 1 級，+7 以上掉 2 級
- 強化後血量上限變高會**即時補上差額**，不會出現「上限變高但血條沒動」
- 背包比較好壞、賣價都改吃強化後的分數（`gearScore()`）
- ⚠ 數值一律走 `gearStat(it, key)`，不要直接讀 `it.atk`——直接讀會漏掉強化倍率

## 主動技能（2026-08-23）
兩顆按鈕就好（Gemini 也是這樣建議，手機按不完更多），各自冷卻、跟等級解鎖，兩種模式都能用。

| 技能 | 解鎖 | 冷卻 | 效果 |
|---|---|---|---|
| 🌀 旋風斬 | Lv3 | 9 秒 | 攻擊 ×2.4（吃魔法倍率），播 `fx_whirl` |
| 💚 治療術 | Lv6 | 22 秒 | 回復最大血量 30%，播 `fx_heal` |

- 冷卻倒數掛在原本 100ms 的 `tick()` 上一起刷，不另外開計時器
- **先播特效、150ms 後才結算傷害**：同一格同時噴命中火花會把技能特效整個蓋掉
- ⚠ 這三張新特效是**單張圖**，當成 1 格的雪碧圖用（`cols:1, rows:1`）。
  `playFx()` 與 `Spr.frame()` 原本算 `cx/(cols-1)`，只有一格時會除以 0 得到 NaN，整張圖不見，
  兩處都要判斷 `cols > 1`

## 已刪除
`games/9x9game-api`（我最早做的獨立後端）在改成共用 lunquiz 帳號之後就沒用到了，
2026-08-23 已刪：Cloudflare 的 Worker `9x9game-api`、D1 資料庫 `9x9game`、本地程式碼全部移除。

## 登入與雲端存檔（2026-08-23）
**帳號跟「小綸出題小幫手」(lunquiz) 共用同一套**（Steve 指定，不另外開一套）。

- 後端：`school/lunquiz/worker`（Worker `linquiz-api`），存檔存它的 D1（`lunquiz-db`）
  - 新增 migration `0005_game_saves.sql`：`game_saves(uid, game, slot, data, updated)`，
    `game` 欄位留著，之後別的遊戲也能共用同一張表
  - 新增 API：`GET/POST/DELETE /api/game/save`、`GET /api/game/slots`（都要 `X-Session-Token`）
  - 單格上限 200KB，slot 1～4
- 前端：`common.js` 的 `Cloud`
  - 註冊走 lunquiz 的 `/api/auth/register`（role 固定 `student`），登入走 `/api/auth/login`
  - **本機只留 token**，密碼打完就送出去不留存
  - `Save.put()` 會觸發 `Cloud.sync()`，節流成最多 4 秒傳一次；關頁面前用 keepalive fetch 補傳
    （⚠ 不能用 `sendBeacon`：它帶不了自訂 header，token 就送不出去）
  - 沒登入照樣能玩，資料留在這台裝置
- **本地也支援四個存檔位**（`mul99_save_s1`…）：`curSlot()`／`sk()`，
  答題統計與各遊戲最佳成績都跟著分格；舊玩家沒有 `_sN` 的資料會自動搬進第 1 格
- ⚠ lunquiz 的帳號**不接受中文**，登入視窗已標示「帳號請用英文或數字」
- ⚠ `findnum.html` 沒吃 common.js，那一頁的紀錄目前不會上雲

**踩過的坑**
- Cloudflare Workers 的 PBKDF2 **迭代上限就是 100000**，寫 200000 會直接丟
  `iteration counts above 100000 are not supported`
- Worker 原始碼裡的正規表示式**不要直接貼中文字面量**（`[一-鿿]`），
  上傳／編碼路上壞掉過，結果是中文帳號一律被擋；要寫成碼點 `一-鿿` 並加 `u` flag
- 我一開始另外建了 `games/9x9game-api`（獨立 Worker + D1 `9x9game`），
  後來 Steve 指定共用 lunquiz，那份就沒用到了（程式碼還留著，Cloudflare 上的 worker 與 D1 待清）

## 2026-08-23 修的四個問題 ＋ 兩個新規則
Steve 回報：怪物沒有攻擊動作、火球是方形的、手機電腦都很卡；另外要角色命名、模式要鎖。

**火球是方形的（真正的原因不是素材）**
`.spell` 與 `.circle` 掛了 `filter:drop-shadow(...)`。這兩張是**不透明的黑底**素材，
drop-shadow 是照 alpha 描邊的，整張圖不透明 → 描出來就是一個矩形光暈，疊上 screen 就變方塊。
**黑底混色素材一律不要加 drop-shadow**，光暈素材裡本來就有。
順手也寫了 `tools/fix_fx.py`：WebP 壓縮會讓黑底浮到 RGB 40 上下，screen 是加亮運算，
那一圈也會微微提亮，腳本把 <=34 壓成 0（純黑像素從 28% 拉到 73%）。
⚠ 這支腳本要靠「有沒有真的透明像素」判斷，不能看 mode：
黑底特效的 PNG 也常存成 RGBA 只是 alpha 整片滿的，看 mode 會全部跳過。
第一版判斷錯，把角色去背圖轉成 RGB（等於毀掉），從 PNG 原檔重存才救回來。

**怪物沒有攻擊動作**
實際量測：靜圖怪的 `mobAtk` 動畫**有在跑**（位移 +45→-96px），但幅度太小又沒有變形與特效，
玩起來就像「滑過來」。改成蓄力壓扁往後 → 拉長撲到 -140px → 過頭回彈，時間拉到 0.62 秒，
命中再加三道爪痕（`clawFx()`）。

**卡頓：改成自動降級**（`Fx` 在 common.js）
量 rAF 幀距，連續 24 幀裡有一半超過 32ms 就掛 `body.lowfx`，只降不升（免得在邊界來回切）。
精簡模式關掉星星／光點／殘影／速度線／角色 drop-shadow，粒子減半，逐幀壓到 12fps。
實測（CPU 放慢模擬）：一般手機 ×4 中位 16.7ms、長幀 2.5%；很弱的機器 ×8 長幀從 41% 降到 20%。
使用者也能手動選：`localStorage` 的 `mul99_fx` ＝ auto／high／low。

**兩個新規則（Steve 2026-08-23 指定）**
- **模式只能在大廳選**，遊戲頁只顯示現在是哪個模式（`Mode.readonly()`），要換得回大廳
- **娛樂模式要密碼 `5407`**（`MODE_PASSWORD`，在 common.js）。切回練習模式不用密碼
- 名稱統一叫「**娛樂模式**」（原本寫純玩模式）
- **角色可以自己取名字**：存 `Save.d.pname`（最多 8 字），空的就用角色本名；
  大廳玩家卡有「✏️ 取名字」，戰鬥血條也吃這個名字（`playerName()`）

## 參考站怎麼做到流暢（shines871/idle-lineage-class，2026-08-22 實際讀過原始碼）
- **邏輯與畫面分離**：戰鬥邏輯是固定 100ms 一個 tick，畫面另外走 `requestAnimationFrame`
- **動畫是預先切好的 PNG 序列**，不是即時算的特效——播放成本低但檔案巨大：
  `assets/anim/` 底下 **7 萬多張 PNG、約 180MB**（我們是把影片抽幀拼成雪碧圖，同樣效果只花 1.1MB）
- 程式切成 20 支檔案（`js/00-data.js` 到 `19-*.js`），最大一支 700KB
- **背景分頁它也跑不動**：程式碼註解自己寫「Chrome 即使降低 setInterval 頻率，
  每次喚醒仍逐 tick 償還已經過的時間」，作法是累積 `_tickDebt`、回到前景**補跑**，
  單次最多補 5 分鐘（`FF_MAX_ELAPSED_MS = 300000`），畫面還會顯示「補跑中，剩餘 …」
  作者後來索性改寫成 C# 安裝版，就是為了繞開這個限制

## 卡關點
- **瀏覽器會節流背景分頁**：切到別的分頁或縮小視窗時，`requestAnimationFrame` 會停、
  `setInterval` 被降到最低 1 秒一次。這是瀏覽器規範層級的限制，網頁版繞不過去。
  參考站作者（放置天堂 Idle Lineage）也是為了這件事才改寫成 C# 安裝版。
  網頁能做的只有「離線推算」：記錄離開時間，回來時用時間差補算收益
- 影片額度一天 3 部（帳號各自獨立），要補逐幀動畫得分天做

---

## 已上線
- 網址：**https://9x9game.pages.dev**（Cloudflare Pages，Steve 在後台綁 GitHub 完成）
- 公開 repo：https://github.com/steveedu711-oss/9x9game （steveedu711-oss，public）
- 發布方式：從 ClaudeOnly 根目錄 `git subtree push --prefix=games/9x9game 9x9game master`
  （`steve0802\gh-accounts.txt` 已加 `games\9x9game|steveedu711-oss|subtree|9x9game|master`，
  在專案目錄跑 `gp` 也可以）
- 線上驗過：四頁 200、無破圖、無 JS 錯誤、手機 390 無溢出；
  打怪掉裝備、存檔跨頁帶到大廳、轉盤出題都正常

## 名稱（2026-08-19 Steve 要求「國小學生在玩的，名字好聽一點」）
- 作品名：**小虎乘法大冒險**（副標「九九乘法闖關 ・ 邊玩邊變強」）
- 七個遊戲：**勇者出擊**（battle，2026-08-22 由「打怪大作戰」改名）／**寶石消消樂**（gems）／**找答案挑戰**（findnum）／
  **飆速賽車**（racing）／**記憶翻牌**（memory）／**雲端跳跳**（cloudjump）／**深海泡泡**（bubble）
- 角色在血條上顯示「小勇者 Lv N」，讓學生代入自己；舊名「九宮虎姬／討伐戰」全部已移除

PowerShell 打 `menu` 選第 7 項（`s9x9`）就會跳到這個專案。

## 檔案結構
| 檔案 | 內容 |
|---|---|
| `index.html` | **大廳**：玩家卡（等級／經驗條／金幣／已裝備）、七張遊戲卡（各自顯示紀錄）、練習狀況統計、清除紀錄 |
| `battle.html` | **勇者出擊**：打怪、升級、掉裝備（主線） |
| `gems.html` | **寶石消消樂**：轉動消除，消完問乘法題 |
| `findnum.html` | **找答案挑戰**：最早的版本，限時 60 秒把答案點掉 |
| `racing.html` | **飆速賽車**：三線道透視賽道，撞正確答案的牌子（2026-08-20 新增） |
| `memory.html` | **記憶翻牌**：算式牌配答案牌，3D 翻面（2026-08-20 新增） |
| `cloudjump.html` | **雲端跳跳**：跳到答案正確的雲上往上爬（2026-08-20 新增） |
| `bubble.html` | **深海泡泡**：canvas 泡泡射擊，限時 60 秒（2026-08-20 新增） |
| `assets/common.css`／`common.js` | 八頁共用：存檔、裝備系統、角色演出、音效、統計、版本列 |

## 存檔（Steve 要求「關掉不會重來」）
`localStorage` 三把 key：
- `mul99_save`：等級、經驗、金幣、身上裝備、背包、關卡進度（round/wave）、累計擊殺
- `mul99_stats`：每個乘法組合的答對／答錯次數（七個遊戲共用）
- `mul99_best_*`：各遊戲最佳成績
程式介面在 `common.js` 的 `Save`（`Save.d` 資料、`Save.put()` 存檔、`Save.power()` 算能力、
`Save.addExp()` 升級）與 `Best`。大廳與討伐戰都有「清除存檔」按鈕。

## 討伐戰的成長系統
- **升級**：打怪得經驗，升一級攻擊 +2、血量 +12，升級當下回 25 血。所需經驗 = 40 + 等級×35
- **掉裝備**：武器／防具／飾品三個部位，稀有度普通→精良→稀有→傳說（顏色不同、數值倍率 1／1.45／2／2.9）。
  稀有度高的多帶一條副屬性。小怪掉落率 45～65%，**Boss 必掉**，關卡越後面越容易出好貨
- **背包**：右上「🎒 裝備」開啟，可換裝或賣掉換金幣；比身上好的會標 ⬆ 並在按鈕上亮紅點
- **屬性影響**：攻擊→傷害、血量→耐打、爆擊→機率 1.8 倍傷害、必殺充能→集氣速度
- 換裝當下若最大血量提高，戰鬥中會即時補上差額

## 寶石轉盤的乘法設計
連通 4 顆以上同款寶石消除 → 跳出「**消掉幾顆 × 每顆幾分 = ?**」，算對拿全部分數、算錯只拿一半。
寶石單價 3／5／6／7／8（刻意避開 2、4，題目才練得到中高段）。
**每轉保證至少一組**（生成後重擲到有為止）：30 格的自然中獎率只有 42%，不保證的話三轉才出一題。

## 動畫（Steve 兩次指出「角色只是換圖很廉價」）
- 虎姬揮擊是**逐格分解動作**：備戰 → 前衝 120px → `fx_hero_swing` → `fx_hero_after` → 斬擊白弧 → 收招
- 受傷：`fx_hero_hurt` ＋ 後仰旋轉 ＋ 紅色閃光 ＋ 震動；怪被打亮白後退、死亡翻滾縮小
- 必殺技：全螢幕暗下＋速度線＋小虎橫向飛過＋大字
- 轉盤：逐欄轉動逐欄停下、中獎格金框脈動、其他格變暗、得分滾動累加（照原版影片逐格分析做的）

## 美術（gemini-auto，帳號 A，全部純黑底生成後去背）
`fx_hero_ready|swing|after|hurt.png`、`fx_cat_attack.png`、`mob_slime|rock|boss.png`、
`princess.png`、`hero_cheer|hero_oops.png`、`cat_jump|cat_wink.png`、`sym_*.png` 9 張寶石。
去背腳本：scratchpad 的 `cut2.py`。

## 驗證（Playwright，走真實點擊）
- 討伐戰：打 3 隻 → Lv2、金幣 78、掉 1 件裝備；裝上護甲血量 112→132；
  關掉重開後 Lv2／金幣 78／關卡 1-4 都接得回來
- 轉盤：一轉出 14 題乘法、得分 225
- 大廳：等級、金幣、裝備、各遊戲紀錄、正確率、最該練的乘法都正確顯示
- 四頁在手機 390 寬都無橫向溢出、全部無 JS 錯誤
- 注意：有無限脈動動畫的元素，Playwright 要 `click(force=True)`

## 角色、怪物、魔法（2026-08-20 Steve：人物太少、怪物太少、沒有魔法、連續答對要有特效）
**四個可選角色**（大廳選，存 `Save.d.hero`，能力寫在 `common.js` 的 `HEROES`）
| 角色 | 特色 | 加成 |
|---|---|---|
| 小虎姬 | 什麼都會一點 | 攻擊 +2（有四格分解動作） |
| 星星法師 | 魔法特別強 | 攻擊 -1／血量 -10／魔法 ×1.45 |
| 勇氣騎士 | 很耐打 | 攻擊 -1／血量 +32 |
| 神射手 | 常常爆擊 | 血量 -6／爆擊 +12% |
只有小虎姬有 ready/swing/after/hurt 四張；其他三個是單張圖，攻擊用 CSS 變形表現。

**怪物 11 隻**（原本 3 隻）：黏黏怪、小蝙蝠、香菇怪、石頭怪、幽靈球、仙人掌、冰晶怪、火焰怪、
鐵皮機器，兩隻 Boss 是虎王石像與紫金巨龍。每隻怕的乘法表都不同，血量與經驗遞增。

**連擊魔法**（連續答對自動施放，這就是「連續答對的特效」）
| 連擊 | 魔法 | 傷害 | 附加 |
|---|---|---|---|
| 3 | 火球術 | 攻擊 ×1.6 | 戰場邊緣泛金光 |
| 5 | 冰霜術 | ×2.2 | 邊緣轉藍 |
| 7 | 落雷術 | ×3.0 | 從天上劈下 |
| 10 | 聖光審判 | ×4.2 | 回血 20，邊緣轉紅；之後每 5 連再放一次 |
施法流程：腳下魔法陣浮現 → 角色被光包住 → 投射物飛過去 → 命中走同一套打擊感 → 大字報連擊。
魔法傷害會再乘上角色的 `spell` 倍率。

**魔法素材要用混色，不要去背**：`fx_fire／ice／thunder／holy／slash_big.png` 是黑底發光素材，
CSS 用 `mix-blend-mode:screen` 讓黑色自然透掉，光暈才完整；去背會把外圈光暈一起吃掉。

**踩過的坑**：選角卡片本來用 inline `onerror="this.outerHTML=..."`，引號轉義把整段 JS 弄壞、
卡片一張都畫不出來（pageerror 還是空字串很難查）。改成 createElement 後再綁 onerror 就好了。

## 打擊感規格（2026-08-20 依業界做法重做，Steve 兩次說效果不夠好）
參考：salivity.github.io 的 game feel 整理、egmatic.com 的 juice 指南、gamejuice.co.uk
- **命中停頓（hitstop）**：一般 80ms、打中弱點 110ms、爆擊 150ms、致命一擊 250ms
  （業界慣例 3～12 幀，致命一擊約 0.25 秒）。停頓只凍角色與特效的動畫，
  **不要連血條 transition 一起凍**，否則殘影效果會失效
- **鏡頭震動要有方向性**：沿攻擊軸線先被推開再回彈，同時輕微推近（zoom punch）；
  三種強度 cam1／cam2／cam3，衰減都很快，畫面才不會糊
- **粒子沿攻擊向量噴**：由左往右扇形 ±55 度，不是四散；一般 10 片、弱點 14、爆擊 20、致命 26
- **攻擊節奏照動畫原理**：預備（往後拉 18px、下蹲）130ms → 衝刺（前衝 132px）110ms
  → 命中 → 收招回位。少了預備動作看起來就會軟
- 衝刺時留兩道殘影＋速度線；刀光拉長成弧形雙層漸層
- 怪物受擊：先被壓扁 → 打飛旋轉 → 晃回；濾鏡全白（亮度 ×7）轉紅再褪回
- 血條白色殘影慢半拍追上；擊殺時 700ms 慢動作＋壓暗
- 打擊音：低頻悶響（頻率下滑）＋高頻白噪碎裂兩層疊

## 打怪動畫大改（2026-08-21，Steve 給的參考站 shines871.github.io/idle-lineage-class）
參考站的關鍵是**逐幀動畫**：每隻怪都有 idle 11～14 幀、attack 7 幀、death 12 幀，還有獨立影子層，
法術也是序列幀 PNG 疊 `screen` 混色。我們原本全是單張靜圖做 CSS 變形，天花板就卡在這裡。

**魔法特效改成真的序列幀**（跟 CSS 漸層畫的假特效不是同一個等級）
- 素材＝Gemini（帳號 A）生的三支 10 秒黑底特效影片，`ffmpeg` 抽幀、Pillow 拼成雪碧圖：
  `assets/fxs_fire.webp`（25 幀 6×5）／`fxs_thunder.webp`（24 幀 6×4）／`fxs_holy.webp`（27 幀 6×5），
  三張加起來只有 420KB。原始 mp4 在 `tools\gemini-auto\2026-08-21\9x9game\`，
  抽幀腳本 `tools\sprite.py`（改參數就能重抽，來源影片路徑寫在檔頭）
- 播放器 `playFx(key, pt, opt)`：逐格換 `background-position`，可指定尺寸、幀率、幀數、色相
- 火球＝原色、**冰霜＝同一張火焰轉成冰藍**（`hue-rotate(168deg)`，省一支影片額度）、
  落雷＝thunder、聖光審判＝holy 光柱；爆擊與致命一擊也補一朵小爆炸
- ⚠ 黑底要靠 `mix-blend-mode:screen` 混掉，**特效層必須跟不透明背景在同一個 stacking context**：
  一開始場景放在 `#world` 外面，特效底下是透明的 → 混出一塊黑方塊。把場景搬進 `#world` 才對

**戰場活起來**
- 新增場景層 `.scene`：天空漸層／星星／遠山／中景樹／地板／飄浮光點，三層視差速度不同
- 遠山與樹是 SVG `mask-image`（inline data URI，無外部請求），顏色吃 CSS 變數 → 換關卡只換變數
- 每一大關換一種景：紫夜森林 → 火山 → 冰原 → 黃金神殿，之後循環（`applyScene()`，`round` 決定）

**鏡頭層 `#world`**
- 戰鬥的東西全包進去，血條與掉落提示留在外面（推近時 UI 不能跟著晃）
- 出手前 `camPull()` 輕輕拉遠、命中 `camPush()` 推近；爆擊、擊殺、Boss 落地推更多
- ⚠ 特效座標基準跟著改成 `#world` 並除以縮放比，不然推近時粒子會飄掉

**Boss 登場過場**（`bossIntro()`，換到 Boss 波次時擋住出題，播完才給題目）
壓暗全場 → Boss 從天上砸下來 → 衝擊波＋揚塵＋碎片＋鏡頭推近 → 名字大字報（含弱點）→ 收場
- ⚠ 名字卡要**自己收**（`nm.innerHTML=''`），不能只靠 CSS 動畫：命中停頓會把動畫 pause 掉，
  它就會賴在畫面上不走

**角色與怪物不再是靜圖**
- 怪物待機呼吸（腳底不動、身體起伏）、角色待機浮動
- 待機小動作：每 3.6～7.2 秒隨機挑一邊晃頭或跳一下
- 怪物攻擊改成「先蓄力往後縮 → 撲過來 → 晃回」，少了預備動作看起來就會軟
- 死亡改成白光溶解＋上飄旋轉縮小，加白光圓環 ×2 與 30 片碎片
- 影子跟著本體演：被打飛壓扁、死掉收乾淨（原本會留一坨黑影在地上）
- 衝刺起步與 Boss 落地會揚塵（`dust()`）

**驗證**（Playwright 走真實點擊，420／1100／360 三種寬度）
四種魔法各截到序列幀在播、Boss 登場過場正常、四頁無 JS 錯誤、無橫向溢出。
額度：A 帳號 2026-08-21 的 3 部影片全部用掉（第一批第三支逾時沒扣額度，補生成功）。

**還沒做**：怪物與角色本身的逐幀（idle／attack／hurt／death 各一組圖），
那是參考站最像「活的」的部分，需要另外生一批素材。

## 四個新遊戲（2026-08-20 Steve：增加遊戲種類、畫質要精美）
先上網查了 timestables.com 這類站台現在的主流玩法（記憶配對、賽車閃避、跳躍過關、跑酷、方塊、
種花園），挑了跟原本三款完全不重疊的四種做。畫風沿用現有 AI 素材，新場景一律用 CSS 做，沒再生圖。

| 遊戲 | 玩法 | 紀錄 key |
|---|---|---|
| 飆速賽車 racing | 三線道，答案牌由遠而近飛來，開到正確那道撞上去。三條命 | `v5_score` |
| 記憶翻牌 memory | 6／8／10 組，金色算式牌配藍色答案牌，全配完算用時與翻牌次數 | `v6_score`／`v6_time` |
| 雲端跳跳 cloudjump | 跳到答案正確的雲上爬一層，爬 20 樓過關。三條命 | `v7_score`／`v7_floor` |
| 深海泡泡 bubble | canvas 泡泡漂浮，限時 60 秒點正確答案射破，射錯扣 3 秒 | `v8_score` |

四款都吃同一套存檔：答對記進 `mul99_stats`、結算給金幣與經驗（`Save.addExp`）、連續答對有連擊字樣、
角色圖跟著大廳選的角色換。難度都是新手／進階／高手三段。

**畫面上踩過的坑**
- 賽車：閘門的水平位移公式是 `LANE_X * s * (W/460)`，車子必須用**同一個係數**（`LANE_X * 1.3 * W/460`，
  1.3 是 p=1 時的 s），不然牌到面前時跟車子對不上。路面車道線也用 `--k` 變數跟著畫面寬度縮放，
  否則手機上線的間距跟車道位移對不起來
- 雲端跳跳：雲朵的凸起是 `::before/::after`，會蓋掉數字，數字要包成 `<span>` 加 `z-index`；
  平台層 top 要拉到 44%，不然會壓到左上角的 HUD
- 記憶翻牌：牌背原本每一組配不同寶石圖，等於**看背面就知道哪兩張是一對**，一定要全部同一款
- 記憶翻牌配錯**不記進答題統計**——那是位置沒記住，不是乘法算錯，記下去會污染「最該再練的乘法」
- 🫧 這個 emoji 在 Windows 10 顯示成空框，改用 🐠

**驗證（Playwright 走真實點擊，四款都無 JS 錯誤）**
賽車三題全對得 42 分且沒扣血、車與閘門中心都在 550；跳跳連跳 3 層得 54 分；泡泡射破 4 顆得 78 分；
翻牌 8 組全配完會跳結算。同一個瀏覽器工作階段玩完回大廳，金幣、等級、各遊戲紀錄、答題統計都帶得回去。
五個頁面在 390 寬都沒有橫向溢出。

## 手機好玩化與最佳化（2026-08-20～21）
Steve 用手機試玩後提的四件事，全部做完。

**手指不小心把畫面放大** → `assets/lock.js`（八頁共用，寫在 `</body>` 前）
- 預設就鎖住：`touch-action:manipulation`（停掉連點兩下放大，不會吃掉一般點擊）
  ＋ 攔 `gesturestart/gesturechange` 與兩指以上的 `touchmove` ＋ viewport 加 `user-scalable=no`
- 右下角一顆 🔒 鈕可以自己解開／鎖回，選擇存 `mul99_lockzoom`，換頁也記得
- **不要用 JS 攔雙擊**（判斷 300ms 內第二次點擊那種寫法），會把遊戲的快速連點一起吃掉

**大廳改版**：玩家卡（頭像＋等級徽章＋經驗條＋金幣膠囊）、分區標題（主線冒險／練習小遊戲）、
遊戲卡改成由 `GAMES` 陣列生成，每款一個代表色（左側光邊、圖示底色、紀錄膠囊都吃同一個 `--ac`）。
**要加新遊戲只改 `index.html` 的 `GAMES` 一處**。

**打怪動畫接順**
- 怪物的漂浮與受擊本來掛同一層，兩個 animation 搶同一個 transform，命中結束會跳一下 →
  漂浮改掛外層 `.mobwrap`、受擊留在 `#mob` 自己身上
- 角色攻擊從五顆 setTimeout 改成**一條 WAAPI 時間軸**（分開排程會各自漂移）
- 命中停頓與擊殺慢動作改成直接 `pause()`／`playbackRate` 控制動畫本身
  （CSS 的 `animation-play-state` 管不到 WAAPI）；**`.bars` 底下的要排除**，血條殘影才追得上
- 手機粒子減四成。實測手機視窗 60fps、沒有超過 50ms 的長幀

**手機版面**：版本列改成一行可左右滑（八個擠成兩三行很難按）、觸控目標一律 ≥44px、
舞台高度補 `dvh`、打怪把角色與怪物各縮一點避免重疊、`overscroll-behavior-y:contain`。

**載入最佳化**
- 素材全部轉 WebP：**14.63 MB → 1.80 MB**（大特效圖只剩 5～7%）。原始 PNG 留著沒刪，
  網頁一律引用 `.webp`；改檔名用的是 `assets/xxx.png → .webp` 的字串替換，
  `findnum.html` 有一行是 `'url(assets/' + c.gem + '.png)'` 這種拼接的，要另外抓
- 打怪本來一進來就預載 11 隻怪＋5 張特效 → 改成先載「角色四格＋目前這隻＋下一隻」，
  其餘等 `load` 後閒下來再補。首屏 1641 KB → **875 KB**
- `setMob()` 原本是「載好了才換圖」，漸進預載後會停在前一隻怪的圖，改成直接指定 src
- 加了 `manifest.json` 與 `assets/icon-192/512.png`，手機可以「加到主畫面」像 App 一樣開

## 音效與背景音樂（2026-08-21）
素材全部是 **CC0 公有領域**（法律上不用標示，但面板裡還是寫了出處）：
- 音樂：Juhani Junkala《Retro Game Music Pack》（OpenGameArt，作者本人在 INFO.txt 聲明 CC0）
  下載的是 49.6 MB 的 wav 包，用 ffmpeg 轉 64k OGG：
  `bgm_main.ogg` 578 KB／74 秒（大廳與小遊戲）、`bgm_battle.ogg` 603 KB／82 秒（打怪）
- 點擊音：Kenney UI Audio（CC0），轉成 mono OGG，每個 4～6 KB

**`assets/audio.js`（八頁共用，在 common.js 之後、lock.js 之前載入）**
- 右下角 🔊 鈕打開面板：音樂／音效各有開關與音量滑桿，設定存 `mul99_audio`，換頁與重開都記得
- 音樂用 `new Audio()`、`preload:'none'`，**點下去才開始下載**，一進站不會先吞掉音樂檔
- 瀏覽器不准沒互動就自動播放，所以第一次點畫面才會出聲（`play()` 被擋就靜靜等下一次）
- 切到別的分頁會自動暫停，切回來繼續
- 按鈕／難度／遊戲卡的點擊音是用 document 上的 pointerdown 代理綁的，**各頁不用自己接**
- `common.js` 那些現場合成的音效（打擊、答對、答錯）改成乘上 `window.__audio.sfx`，
  所以音效滑桿對它們也有效；`findnum.html` 自帶一份 beep，也一起接上了

**踩過的坑**
- 音樂是 `new Audio()` 建的、**不在 DOM 裡**，`document.querySelectorAll('audio')` 抓不到，
  驗證時會誤判成「沒在播」。所以留了 `window.__audio.state()` 這個查狀態的出口
- 面板裡的 `input[type=range]` 要加 `min-width:0`，不然 flex 容器裡會撐破面板

## 打怪再打磨：前搖預告、遠程與召喚、掉寶演出（2026-08-22 晚）
- **出手前搖預告**：怪物倒數剩 1.2 秒時繃緊身體＋頭上跳一顆驚嘆號。
  沒有預告的攻擊只會讓人覺得被偷打，有預告才有「看懂就躲得掉」的手感（純玩模式尤其明顯）
- **遠程型**（仙人掌 🎯）：不撲過來，改成噴一發綠色投射物，飛行 420ms 才命中，看得到才躲得掉
- **召喚型**（蜂后 🐝）：護盾被打光才會再召一次，護盾＝本體血量 22%，
  傷害先吃護盾、本體不掉血，血條旁會顯示「護盾 N」，身上罩一層藍光殼
- **掉寶演出**：裝備從怪物身上噴出來、轉一圈飛進右上角背包，背包按鈕跟著彈一下，
  發光顏色吃裝備稀有度。原本只跳一行字，看不出「打到寶」
- 怪物特性一律標在名字後面（⚡ 出手快／🛡 有護甲／🎯 遠程／🐝 會召喚），玩家才知道為什麼這隻不一樣

**驗證**：八頁無 JS 錯誤／無溢出；打怪頁手機模擬（CPU ×4）中位 16.7ms、長幀 1.9%；
用密集取樣抓到預告、投射物、護盾、掉寶動畫都真的有出現（單看某個時間點會抓不到，特效只存在幾百毫秒）。

## 兩種模式、題型可選、新素材、手機效能（2026-08-22 下午）
Steve 這一輪要的：**兩種模式並存**（不是二選一）、低年級要能練 20 以內加減、
手機玩會卡、以及參考天堂／楓之谷把「不用答題時的樂趣」補起來。

**兩種模式（八頁共用，選擇存 `mul99_mode`）**
- `practice` 練習模式：出算術題（原本的玩法）
- `arcade` 純玩模式：不出題，改成純操作，**不寫入答題統計**（`mul99_stats` 不會被污染）
- 切換介面 `Mode.bar()` 在 `common.js`，大廳與每一頁的開始畫面都放得下
- 各遊戲的純玩玩法：
  | 遊戲 | 純玩模式 |
  |---|---|
  | 勇者出擊 | 自己按「攻擊」「閃避」，敵人倒數完撲上來；閃避有 0.7 秒無敵、冷卻 2.4 秒；連續命中照樣累積連擊放魔法；三段難度（悠閒／普通／狠角色）差在敵人出手間隔與傷害 |
  | 寶石消消樂 | 不出題，消到多少直接全額給分 |
  | 找答案挑戰 | 直接給一個數字，把盤面上一樣的點掉 |
  | 飆速賽車 | 三道只有一道沒路障，越開越快（每五題縮短 120ms，最低 1.1 秒） |
  | 記憶翻牌 | 配同一張圖（純記性），不再是算式配答案 |
  | 雲端跳跳 | 跳有 ⭐ 的雲，其他一踩就碎；樓層越高整排飄得越快 |
  | 深海泡泡 | 指定一種顏色，把該色泡泡射破 |
- 鍵盤：打怪純玩模式 **A** 攻擊、**S** 閃避、**空白鍵**必殺

**題型可選（存 `mul99_topic`，八頁共用）**
`common.js` 的 `makeQ(weak)` 與 `makeOptions(q, n)` 是唯一出題器，五種題型：
九九乘法／加法（20 以內）／減法（20 以內、不出負數）／加減混合／全部混合。
- 統計 key 直接用算式（`7＋8`、`12－5`、`5×3`），三種題型的統計自然分開
- 乘法時仍沿用各遊戲原本的出題範圍（難度分段才有意義），非乘法才走共用出題器
- ⚠ `findnum.html` **沒有吃 common.js**（最早的版本自帶一份），題型的邏輯與樣式都自己複製了一份，
  改共用出題器時**兩邊都要改**
- ⚠ findnum 的干擾數字原本是「所有乘積」，加減法要換成 0～22，不然答案一眼就看出來
- 寶石消消樂的題目本來就是「幾顆 × 每顆幾分」，情境本身是乘法，維持乘法不給選題型

**新素材（Gemini 帳號 A 生圖 9 張，一次 `--batch` 生完）**
`tools/cut_art.py` 去背轉 webp（**生圖有時給白底不是黑底**，腳本會自己看角落決定挖哪一種；
`fx_` 開頭的特效不去背，留黑底給 `mix-blend-mode:screen`）。
- 新怪四隻：暴走野豬（`fast:0.75` 出手快）、蜂后、影武者貓（`fast:0.8`）、
  石像守衛（`armor:0.72` 受到的傷害打七折，Boss 級精英）。名字後面會標 ⚡ 或 🛡 讓玩家看得出差異
- 新角色兩個：狂戰小虎（攻擊 +6／血量 -24）、魔導小虎（魔法 ×1.7／必殺充能 +7）
- 新特效三張：旋風斬、毒霧、治療（還沒接進技能系統）

**手機效能（實測用 Playwright 把 CPU 放慢四倍，量每一幀的間隔）**
| 頁面 | 之前 | 現在 |
|---|---|---|
| 打怪 | 中位 33ms、13.8% 長幀 | 中位 16.7ms、1.1% |
| 賽車 | 中位 66.7ms、87% 長幀 | 中位 16.7ms、0% |
| 泡泡 | 中位 33.3ms | 中位 16.7ms |
三個真正的元凶：
1. **整片背景漸層的漂移動畫**（`body::before` 的 `drift`）：一秒六十次重畫整個畫面，手機直接砍半 fps。手機關掉
2. **角色與怪物的 `drop-shadow`**：逐幀每換一格都要重算整張圖的陰影。手機關掉，地上本來就有 `.gshadow` 影子
3. **賽車路面的 `background-position` 動畫**：帶 3D 變形又是整片重繪。改成獨立一層用 `transform` 位移（走合成不重繪）
另外泡泡的 canvas `shadowBlur` 每顆泡泡都要模糊一次，小螢幕一律關掉

**跟 Gemini 討論的結論（帳號 A，12 輪）**
⚠ 這一輪 A 帳號**沒有 Thinking**（專業版授權在 B），品質比平常低一些。
可用的重點：拿掉答題之後，樂趣要靠「數值爆破的視覺回饋、噴寶的隨機驚喜、
高風險高報酬的強化、怒氣爆發的大招」四根柱子撐；怪物要有辨識度就得給不同**行為**
（衝鋒／護盾／遠程／召喚），而且要有前搖預告；技能不要超過兩顆按鈕。
生圖提示詞也是它寫的（`chibi Q-version + thick black outline + solid black background` 三個關鍵字鎖風格）。
完整逐字稿在 scratchpad 的 `gemini_talk.md`，分頁留著沒關給 Steve 看。

**Steve 給的參考**：巴哈的《放置天堂 Idle Lineage》（也是 AI 做出來的，作者持續改版中）。
可借鏡的地方：它也是**兩種模式並存**（一般／經典，經典是原版能力、死亡會噴經驗），
其餘像仇恨值系統（怪物打誰看仇恨高低，職業×武器×傷害類型決定）、套裝與遺物、
技能轉換（同一招換成另一招而不是加新按鈕）、離線收益、NPC 會在世界頻道互嗆，
都是「數值以外的樂趣」怎麼長出來的實例。作者自己的心得也值得記：
既有框架的東西做完之後，要繼續有樂趣就得整個架構魔改（例如取消強化卷軸、重做職業技能與轉職）。

**還沒做**：怪物的行為原型只做了「出手快」與「有護甲」兩種，遠程與召喚還沒做；
新的三張技能特效還沒接進技能系統；純玩模式還沒有 Gemini 建議的強化系統與怒氣大招。

## 逐幀角色與怪物、怪物攻擊、大廳改版（2026-08-22）
Steve 三件事：畫面要更精緻專業（用 Gemini A 生素材，打怪優先）、大廳 UI/UX 不夠舒服、
「沒有怪物攻擊的動畫」。

**素材（Gemini 帳號 A，當天 3 部影片全部用掉）**
黑底、鎖死鏡頭、角色置中、一鏡到底的動作循環影片三支，原始檔在
`tools\gemini-auto\2026-08-22\9x9game\`（hero_attack／dragon_attack／slime_attack）。
抽幀去背腳本 **`tools/sprite_char.py`**（跟做特效的 `sprite.py` 不同：特效留黑底靠
`mix-blend-mode:screen` 混掉，角色要疊在場景上，黑底一定要真的變透明）。
- 去背要**兩段一起做**：從四邊 flood fill 挖外圍的黑，再用「侵蝕→膨脹」挖翅膀底下、
  四肢之間那種被圍住的大塊黑。只做 flood 會留一塊黑；只用亮度門檻會把角色的黑描邊一起吃掉
- 產出六張：`spr_hero_idle/atk`、`spr_slime_idle/atk`、`spr_dragon_idle/atk`（共約 1.1MB）
- ⚠ 首格圖轉影片（`--image`）在影片模式下**找不到上傳入口會逾時**（額度不會扣），
  角色一致性改用提示詞描述外觀達成

**接進 battle.html：不新增 DOM**
`#hero2`／`#mob` 這兩個 `<img>` 掛上 `sprmode` 就變成雪碧圖的視窗——src 換成透明像素、
改用 background 顯示、寬度靠 `aspect-ratio` 算。定位、drop-shadow、受擊／死亡那些既有
class 全部照舊能用，播放器是 `Spr`（`Spr.play/on/off`）。
- ⚠ 呼吸動畫要改寫成 `#mob:not(.sprmode)`，**不能寫 `#mob.sprmode{animation:none}`**，
  那樣會把受擊與死亡動畫一起關掉
- 殘影 `afterImage()` 在逐幀模式要連 background 一起複製，不然複製到的是透明像素
- 小虎姬用攻擊序列的第 7～22 格（揮擊那段）@45fps，正好對上 `heroSwing` 的 620ms 節奏

**怪物攻擊**（Steve：看不出怪物有在攻擊）
- 黏黏怪與紫金巨龍改播自己的攻擊逐幀（`MOB_ATK` 定每隻的起訖格、幀率、命中在整段的幾成）
- 逐幀只在自己那一格裡動，**不會真的靠近角色**，位移另外補一條 WAAPI 時間軸
  （蓄力往後 → 撲到面前 → 回彈），命中點對到牠真的撲到／噴到的那一格
- 命中角色補 `mobHitFx()`：轉紅的爆散＋衝擊圓環＋碎片。碎片 `shards()` 加了第四個參數
  `dir`，怪打過來時往左噴（原本寫死由左往右，那是角色打怪的方向）
- 這一段對**所有** 11 隻怪都有效，沒有逐幀的那 9 隻也比原本只有位移好認得多

**大廳改版**
- 桌機（≥900px）改兩欄：左欄 sticky 放玩家卡與選角（四張改排兩排，字不再被折斷），
  右欄放主線大卡、練習小遊戲（兩欄格狀）、練習狀況。整頁不用一直往下滑
- 主線卡加大加重（圖示 76px、標題 22px），右側壓一隻半透明紫金巨龍當裝飾
  ⚠ `.game::after` 原本是 `inset:0` 的掃光層，改用時**一定要補 `left:auto`**，
  否則 `left:0` 會贏過 `width`，裝飾整個貼到左邊壓在文字上
- 手機把練習小遊戲的說明壓成一行、卡片變矮，整頁短了一大截
- 左下那張大立繪只在 ≥1400px 才出現（1080～1400 會壓到左欄）

**驗證（Playwright 走真實點擊）**
八頁 × 桌機 1280／手機 390：無 JS 錯誤、無 404、無橫向溢出。
打怪走完整流程：角色揮擊逐幀正常、答錯時黏黏怪蓄力→撲擊→命中特效、
巨龍 Boss 待機拍翅與吐息都有播、打死換成沒有逐幀的小蝙蝠會正確切回靜圖模式。
另外量過怪物位移時間軸：蓄力 +34px、撲擊 -148px，命中點落在最靠近的那一格。

**還沒做**：其他 9 隻怪與另外三個角色仍是單張靜圖（影片額度一天 3 部，當天已用完）。
補齊要再生 3～4 支影片，同一套流程跑 `tools/sprite_char.py` 就好。
另外戰鬥中的小虎姬已經是 Q 版逐幀，大廳的選角卡與頭像還是舊的寫實立繪，
等其他角色的逐幀補齊時一起換成同一種風格。

## 下一步（Steve 還沒指定）
0. 檔名（2026-08-20 Steve 指定）：大廳才叫 index.html，其他一律用玩法命名——
   `battle`／`gems`／`findnum`／`racing`／`memory`／`cloudjump`／`bubble`。
   **`findnum.html` 沒有吃 `assets/common.js`**（最早的版本，程式自帶一份），
   它的版本列是寫死的連結，改檔名時要另外手動改那一段。
1. 名字若要再換，八個 html 的 `<title>`／`<h1>` 與 `common.js` 的 `VERSIONS` 一起改
2. 金幣目前只能靠賣裝備累積，還沒有商店可以花（可做：買血藥、抽裝備、換造型）
3. 更多敵人與招式、關卡地圖；新四款目前只有三段難度，還沒有關卡進度
4. 要不要發布到 Cloudflare Pages（對外，發布前要問 Steve）

## 卡關點
無。

## 踩過的坑
- gemini-auto 批次 TSV 的輸出路徑在 Python 裡**一定要用 raw string**，`\a` 會變控制字元，
  圖生成成功卻全部存檔失敗（額度照扣）
- 用 python 大段替換 HTML 內的 `<script>` 時，要確認那次指令真的執行成功；
  有一次被中斷，結果版面換了、邏輯沒換，畫面看起來正常但一按就報 null
